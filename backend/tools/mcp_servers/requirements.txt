# MCP Servers - Python Dependencies
# Shared across all MCP tool servers

# Core MCP Framework (Official SDK)
mcp>=0.1.0

# Web server and ASGI framework
uvicorn>=0.24.0
starlette>=0.35.0

# Async HTTP clients
httpx>=0.25.0

# LLM integration
openai>=1.0.0

# Code interpreter sandbox
docker>=7.0.0

# HTML content extraction
# Trafilatura: F1 0.958 (vs ReadabiliPy ~0.92) - used by HuggingFace, IBM, Microsoft
trafilatura>=2.0.0

# Rule-based HTML to Markdown conversion (fast, no LLM needed)
markdownify>=0.14.1

# Image processing (for ComfyUI Qwen server)
Pillow>=10.0.0

# Data validation
pydantic>=2.0.0

# Testing (optional, only needed for development)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
